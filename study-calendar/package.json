{
  "name": "study-calendar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Personal IBDP summer study calendar — single static page, no backend.",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && vite build --configLoader runner",
    "preview": "vite preview --configLoader runner"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "tailwindcss": "^4.1.0",
    "typescript": "~5.8.0",
    "vite": "^6.3.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
